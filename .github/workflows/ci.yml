# # workflow的名称，会显示在工作流运行页面
# name: koa-server-fc

# # 工作流执行的契机：push表示每次push代码之后都会执行
# on: [push]

# jobs:
#   build:
#     # 构建运行的环境
#     runs-on: ubuntu-latest
#     # 构建步骤
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3.0.0
#       - uses: actions/setup-node@v3
#         with:
#           node-version: '14'
#       - name: push to aliyun fc
#         run: npm install
#       - run: npm install -g @serverless-devs/s
#       - run: s config add --AccessKeyID ${{secrets.ACCESS_KEY_ID}} --AccessKeySecret ${{secrets.ACCESS_KEY_SECRET}} -a jcl
#       - run: s deploy --use-remote

